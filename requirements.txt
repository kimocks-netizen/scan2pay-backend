# Web framework
fastapi==0.115.0
mangum==0.19.0

# Database — Supabase (Postgres via postgrest + auth)
supabase==2.10.0

# Validation
pydantic==2.9.0
pydantic-settings==2.5.0

# Auth
python-jose[cryptography]==3.5.0
bcrypt==4.2.0
passlib[bcrypt]==1.7.4
requests==2.33.0
google-auth==2.37.0
pyasn1==0.6.4  # transitive via python-jose/google-auth — pinned to clear known CVEs

# HTTP client (Paystack, WinSMS)
httpx==0.27.0

# AWS SDK (S3, SSM, Lambda)
boto3==1.35.0

# Utilities
python-multipart==0.0.31
